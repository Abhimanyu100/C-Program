#include <stdio.h>
int main()
{
    int a, b;
    printf("Enter two number: ");
    scanf("%d %d", &a, &b);
    if()
    {

        printf("%d and %d numbers are co-prime", a, b);
    }
    else
    {
        printf("%d and %d numbers are not co-prime", a, b);
    }

}
